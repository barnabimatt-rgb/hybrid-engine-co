// frontend/src/index.jsx — React entry point (for future build system)
// Currently the dashboard is served as a static HTML file in /public/index.html
// This file exists for future migration to a React build pipeline
console.log('Hybrid Engine Co. Dashboard — React entry point');
console.log('Currently served as static HTML. See /public/index.html');
