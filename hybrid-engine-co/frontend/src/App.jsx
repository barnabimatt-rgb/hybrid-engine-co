// frontend/src/App.jsx — Main app shell (for future React build)
// Currently the dashboard runs as vanilla JS in /public/index.html
// This file provides the React component structure for future migration
export default function App() {
  return null; // See /public/index.html for the active dashboard
}
